# --> This file contains relevant constant data that will be used by other scripts

from libqtile.config import Group

default_terminal = 'wezterm'

# create the groups
groups = [Group(i) for i in "123456789"]
